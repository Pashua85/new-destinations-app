import React from 'react';

const Header = () => (
  <header className="page-header">
    <div className="container">
      <h1 className="page-header__title">Destinations app</h1>
    </div>
  </header>
);

export default Header;